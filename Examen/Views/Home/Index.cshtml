@model IEnumerable<Examen.BE.Producto>


@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    @using (Html.BeginForm("Index","Home"))
    {
        <h1>Aprovecha nuestras ofertas</h1>
        <label>Buscar:</label> <input type="text" name="nombre" class="form-control"/>
        <br />
        <input type="submit" value="Buscar" name="Buscar" class="btn btn-default" />
    }

</div>

<div class="row">


    @foreach (var item in Model)
    {
    <div class="form-group col-sm-4">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </th>
            </tr>
            <tr>
                <td>
                    <div class="form-group">
                        @Html.DisplayFor(modelItem => item.Marca.ToList()[0].Descripcion)
                    </div>
                    <div class="form-group">
                        @Html.DisplayFor(modelItem => item.Cuv)
                    </div>
                    <div class="form-group">
                        S/.@Html.DisplayFor(modelItem => item.Precio)
                    </div>
                    <div class="form-group">
                        <select class="form-control" id="sel1">
                            <option>1</option>
                            <option>2</option>
                            <option>3</option>
                            <option>4</option>
                        </select>
                        @Html.ActionLink("Comprar", "comprar", new { nombre = "1" })
                    </div>

                </td>
            </tr>
        </table>
    </div>
            }


        </div>
